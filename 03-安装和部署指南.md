# TiDB 安装和部署指南

## 1. 部署前准备

### 1.1 系统要求

#### 1.1.1 硬件要求

**开发环境（最小配置）**：
- CPU：4核
- 内存：8GB
- 磁盘：50GB SSD
- 网络：千兆网卡

**生产环境推荐配置**：
- TiDB Server：8核 + 32GB内存
- TiKV Server：16核 + 64GB内存 + 500GB SSD
- PD Server：4核 + 8GB内存 + 100GB SSD

#### 1.1.2 软件要求

**操作系统**：
- CentOS 7.6+ / Red Hat 7.6+
- Ubuntu 16.04+
- macOS 10.13+

**依赖软件**：
```bash
# CentOS
yum install -y curl tar gcc

# Ubuntu
apt-get update
apt-get install -y curl tar gcc

# macOS
brew install curl
```

#### 1.1.3 网络配置

**端口要求**：
- TiDB Server：4000（客户端连接）
- TiKV Server：20160（节点间通信）
- PD Server：2379（客户端）、2380（节点间通信）
- Grafana：3000（监控界面）

**防火墙配置**：
```bash
# CentOS 开放端口
firewall-cmd --permanent --add-port=4000/tcp
firewall-cmd --permanent --add-port=20160/tcp
firewall-cmd --permanent --add-port=2379/tcp
firewall-cmd --reload

# Ubuntu 开放端口
ufw allow 4000/tcp
ufw allow 20160/tcp
ufw allow 2379/tcp
```

### 1.2 环境检查

```bash
# 检查系统信息
uname -a
cat /etc/os-release

# 检查内存和磁盘
free -h
df -h

# 检查网络连通性
ping google.com
curl https://www.pingcap.com

# 检查时区设置
timedatectl status
```

## 2. 本地开发环境部署

### 2.1 Docker 快速部署（推荐）

#### 2.1.1 单机 Docker 部署

```bash
# 拉取 TiDB 镜像
docker pull pingcap/tidb:v7.1.0

# 创建数据目录
mkdir -p ~/tidb-data/{tikv,pd,tidb}

# 启动 PD
docker run -d --name pd \
    -p 2379:2379 \
    -v ~/tidb-data/pd:/data \
    pingcap/pd:v7.1.0

# 启动 TiKV
docker run -d --name tikv \
    -p 20160:20160 \
    -v ~/tidb-data/tikv:/data \
    --link pd:pd \
    pingcap/tikv:v7.1.0 \
    --addr="0.0.0.0:20160" \
    --advertise-addr="127.0.0.1:20160" \
    --data-dir="/data" \
    --pd="pd:2379"

# 启动 TiDB
docker run -d --name tidb \
    -p 4000:4000 \
    -p 10080:10080 \
    --link pd:pd \
    --link tikv:tikv \
    pingcap/tidb:v7.1.0 \
    --store=tikv \
    --path="pd:2379"

# 检查状态
docker ps
```

#### 2.1.2 Docker Compose 部署

创建 `docker-compose.yml` 文件：

```yaml
version: '3.8'

services:
  pd:
    image: pingcap/pd:v7.1.0
    ports:
      - "2379:2379"
      - "2380:2380"
    volumes:
      - ./data/pd:/data
    command:
      - --name=pd
      - --data-dir=/data
      - --client-urls=http://0.0.0.0:2379
      - --peer-urls=http://0.0.0.0:2380
      - --advertise-client-urls=http://127.0.0.1:2379
      - --advertise-peer-urls=http://127.0.0.1:2380
      - --initial-cluster=pd=http://127.0.0.1:2380

  tikv:
    image: pingcap/tikv:v7.1.0
    ports:
      - "20160:20160"
    volumes:
      - ./data/tikv:/data
    depends_on:
      - pd
    command:
      - --addr=0.0.0.0:20160
      - --advertise-addr=127.0.0.1:20160
      - --data-dir=/data
      - --pd=pd:2379

  tidb:
    image: pingcap/tidb:v7.1.0
    ports:
      - "4000:4000"
      - "10080:10080"
    depends_on:
      - pd
      - tikv
    command:
      - --store=tikv
      - --path=pd:2379
      - --host=0.0.0.0
      - --port=4000
      - --status-addr=0.0.0.0:10080
```

启动集群：

```bash
# 启动集群
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止集群
docker-compose down
```

### 2.2 TiUP Playground 本地部署

#### 2.2.1 安装 TiUP

```bash
# 安装 TiUP
curl --proto '=https' --tlsv1.2 -sSf https://tiup-mirrors.pingcap.com/install.sh | sh

# 设置环境变量
source ~/.bash_profile
```

#### 2.2.2 启动 Playground

```bash
# 启动本地集群（默认版本）
tiup playground

# 指定版本启动
tiup playground v7.1.0

# 自定义配置启动
tiup playground v7.1.0 --db 2 --kv 3 --pd 3 --host 0.0.0.0

# 启用监控
tiup playground v7.1.0 --monitor

# 查看集群状态
tiup playground display

# 停止集群
tiup playground stop
```

### 2.3 源码编译部署

#### 2.3.1 编译环境准备

```bash
# 安装 Go 1.19+
wget https://golang.org/dl/go1.19.linux-amd64.tar.gz
sudo tar -C /usr/local -xzf go1.19.linux-amd64.tar.gz
export PATH=$PATH:/usr/local/go/bin

# 验证安装
go version

# 设置 Go 代理
export GOPROXY=https://goproxy.cn,direct
```

#### 2.3.2 编译 TiDB

```bash
# 克隆源码
git clone https://github.com/pingcap/tidb.git
cd tidb

# 编译
make

# 编译特定组件
make tikv
make pd
make tidb-server

# 验证编译结果
ls -la bin/
```

#### 2.3.3 启动开发集群

```bash
# 启动 PD
./bin/pd-server --name=pd --data-dir=pd-data --client-urls=http://127.0.0.1:2379 &

# 启动 TiKV
./bin/tikv-server --pd-endpoints=http://127.0.0.1:2379 --addr=127.0.0.1:20160 --data-dir=tikv-data &

# 启动 TiDB
./bin/tidb-server --store=tikv --path="127.0.0.1:2379" --port=4000 &

# 验证连接
mysql -h 127.0.0.1 -P 4000 -u root
```

## 3. 生产环境部署

### 3.1 TiUP 集群部署

#### 3.1.1 安装 TiUP Cluster

```bash
# 安装 TiUP Cluster
tiup cluster

# 更新组件
tiup update --self
tiup update cluster
```

#### 3.1.2 准备配置文件

创建 `topology.yaml` 文件：

```yaml
global:
  user: "tidb"
  ssh_port: 22
  deploy_dir: "/tidb-deploy"
  data_dir: "/tidb-data"

server_configs:
  tidb:
    log.slow-threshold: 300
  tikv:
    storage.reserve-space: "1GB"
  pd:
    replication.max-replicas: 3

pd_servers:
  - host: 10.0.1.1
  - host: 10.0.1.2
  - host: 10.0.1.3

tidb_servers:
  - host: 10.0.1.4
  - host: 10.0.1.5

tikv_servers:
  - host: 10.0.1.6
  - host: 10.0.1.7
  - host: 10.0.1.8

tiflash_servers:
  - host: 10.0.1.9
    data_dir: "/tidb-data/tiflash"
    config:
      logger.level: "info"

monitoring_servers:
  - host: 10.0.1.10

grafana_servers:
  - host: 10.0.1.10
```

#### 3.1.3 部署集群

```bash
# 检查目标服务器
tiup cluster check topology.yaml --apply

# 部署集群
tiup cluster deploy tidb-cluster v7.1.0 topology.yaml --user root

# 启动集群
tiup cluster start tidb-cluster

# 查看集群状态
tiup cluster display tidb-cluster
```

### 3.2 Kubernetes 部署

#### 3.2.1 安装 TiDB Operator

```bash
# 安装 Helm
curl https://raw.githubusercontent.com/helm/helm/master/scripts/get-helm-3 | bash

# 添加 PingCAP 仓库
helm repo add pingcap https://charts.pingcap.org/
helm repo update

# 安装 TiDB Operator
helm install --namespace tidb-admin tidb-operator pingcap/tidb-operator
```

#### 3.2.2 创建 TiDB 集群

创建 `tidb-cluster.yaml` 文件：

```yaml
apiVersion: pingcap.com/v1alpha1
kind: TidbCluster
metadata:
  name: basic
  namespace: default
spec:
  version: v7.1.0
  timezone: UTC
  pvReclaimPolicy: Delete
  enableDynamicConfiguration: true
  configUpdateStrategy: RollingUpdate
  discovery: {}

  pd:
    baseImage: pingcap/pd
    replicas: 3
    requests:
      storage: "10Gi"
    config: |
      [log]
        level = "info"

  tikv:
    baseImage: pingcap/tikv
    replicas: 3
    requests:
      storage: "100Gi"
    config: |
      [log]
        level = "info"
      [storage]
        reserve-space = "1GB"

  tidb:
    baseImage: pingcap/tidb
    replicas: 2
    service:
      type: ClusterIP
    config: |
      [log]
        level = "info"
      [performance]
        tcp-keep-alive = true

  tiflash:
    baseImage: pingcap/tiflash
    replicas: 1
    requests:
      storage: "50Gi"

  monitor:
    prometheus:
      baseImage: prom/prometheus
      version: v2.27.1
    grafana:
      baseImage: grafana/grafana
      version: 7.5.11
    initializer:
      baseImage: pingcap/tidb-monitor-initializer
      version: v7.1.0
```

部署集群：

```bash
# 应用配置
kubectl apply -f tidb-cluster.yaml

# 查看集群状态
kubectl get pods --watch
kubectl get tidbclusters basic

# 连接 TiDB
kubectl port-forward svc/basic-tidb 4000:4000
mysql -h 127.0.0.1 -P 4000 -u root
```

### 3.3 云平台部署

#### 3.3.1 TiDB Cloud 部署

1. **注册 TiDB Cloud 账户**
   - 访问 https://tidbcloud.com/
   - 免费注册或登录

2. **创建集群**
   ```bash
   # 使用 TiUP Cloud CLI
   tiup cloud

   # 查看可用区域
   tiup cloud region list

   # 创建免费集群
   tiup cluster create cloud my-cluster --plan free
   ```

3. **连接集群**
   ```bash
   # 获取连接信息
   tiup cloud cluster list

   # 连接集群
   mysql -h {host} -P 4000 -u root -p
   ```

#### 3.3.2 AWS RDS 兼容部署

```bash
# 使用 AWS CLI 部署
aws rds create-db-instance \
    --db-instance-identifier tidb-cluster \
    --db-instance-class db.m5.large \
    --engine mysql \
    --master-username admin \
    --master-user-password securepassword \
    --allocated-storage 100 \
    --vpc-security-group-ids sg-12345678 \
    --availability-zone us-east-1a
```

## 4. 集群管理和维护

### 4.1 基础运维操作

#### 4.1.1 集群状态检查

```bash
# TiUP 集群管理
tiup cluster display tidb-cluster
tiup cluster list
tiup cluster audit

# 查看组件状态
tiup cluster exec tidb-cluster --command "systemctl status tidb-4000"
tiup cluster exec tidb-cluster --command "systemctl status tikv-20160"

# 查看日志
tiup cluster tail tidb-cluster --component=tidb
tiup cluster tail tidb-cluster --component=pd
```

#### 4.1.2 扩缩容操作

```bash
# 添加 TiDB 节点
tiup cluster scale-out tidb-cluster scale-out.yaml

# 添加 TiKV 节点
tiup cluster scale-out tidb-cluster scale-out-tikv.yaml

# 缩容节点
tiup cluster scale-in tidb-cluster -N 10.0.1.5:4000

# 执行缩容
tiup cluster prune tidb-cluster
```

#### 4.1.3 升级集群

```bash
# 检查升级版本
tiup cluster check tidb-cluster --cluster v7.1.1

# 执行升级
tiup cluster upgrade tidb-cluster v7.1.1

# 升级过程中查看进度
tiup cluster display tidb-cluster

# 回滚升级
tiup cluster rollback tidb-cluster v7.1.0
```

### 4.2 备份和恢复

#### 4.2.1 使用 BR 工具备份

```bash
# 安装 BR 工具
tiup install br

# 全量备份
tiup br backup full \
    --pd "127.0.0.1:2379" \
    --storage "s3://backup-data?access-key=minio&secret-key=minio123" \
    --ratelimit 128 \
    --log-file backup-full.log

# 增量备份
tiup br backup full \
    --pd "127.0.0.1:2379" \
    --storage "s3://backup-data?access-key=minio&secret-key=minio123" \
    --lastbackupts "2024-01-01 00:00:00" \
    --ratelimit 128

# 恢复数据
tiup br restore full \
    --pd "127.0.0.1:2379" \
    --storage "s3://backup-data?access-key=minio&secret-key=minio123" \
    --ratelimit 128
```

#### 4.2.2 使用 Dumpling 导出

```bash
# 导出数据
tiup dumpling \
    -u root \
    -P 4000 \
    -B test_db \
    -o /data/backup \
    -F 256MB \
    -r 200000 \
    --threads 16

# 导出特定表
tiup dumpling \
    -u root \
    -P 4000 \
    -B test_db \
    -T test_db.users \
    -o /data/backup-users \
    --threads 8
```

### 4.3 监控和告警

#### 4.3.1 配置 Grafana 监控

```bash
# 访问 Grafana 界面
http://<grafana-ip>:3000

# 默认凭据
Username: admin
Password: admin

# 添加 TiDB 监控面板
# 1. 导入 TiDB Dashboard (ID: 14340)
# 2. 配置 Prometheus 数据源
# 3. 设置告警规则
```

#### 4.3.2 配置告警规则

```yaml
# alertmanager.yml
global:
  smtp_smarthost: 'localhost:587'
  smtp_from: 'alert@example.com'
  smtp_auth_username: 'alert@example.com'
  smtp_auth_password: 'password'

route:
  group_by: ['alertname', 'cluster']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 12h
  receiver: 'web.hook'

receivers:
- name: 'web.hook'
  email_configs:
  - to: 'admin@example.com'
```

## 5. 最佳实践和注意事项

### 5.1 部署最佳实践

#### 5.1.1 硬件选择建议

**TiDB Server**：
- SSD 磁盘用于日志存储
- 充足的内存用于查询缓存
- 高性能 CPU 用于 SQL 处理

**TiKV Server**：
- 高性能 NVMe SSD
- 大内存用于 Block Cache
- 多核 CPU 用于 Raft 处理

**PD Server**：
- SSD 磁盘用于元数据存储
- 足够的网络带宽

#### 5.1.2 网络配置

```bash
# 配置网络参数
echo 'net.core.somaxconn = 32768' >> /etc/sysctl.conf
echo 'net.ipv4.tcp_tw_reuse = 1' >> /etc/sysctl.conf
echo 'net.ipv4.tcp_fin_timeout = 10' >> /etc/sysctl.conf

# 应用配置
sysctl -p
```

#### 5.1.3 安全配置

```bash
# 配置防火墙规则
firewall-cmd --permanent --add-rich-rule='rule family="ipv4" source address="10.0.0.0/8" accept'
firewall-cmd --permanent --add-rich-rule='rule family="ipv4" source address="172.16.0.0/12" accept'

# 配置 SSL/TLS
# 1. 生成证书
# 2. 配置集群使用 TLS
# 3. 配置客户端连接使用 TLS
```

### 5.2 常见问题和解决方案

#### 5.2.1 部署失败排查

```bash
# 检查集群部署日志
tiup cluster audit tidb-cluster

# 检查节点状态
tiup cluster exec tidb-cluster --command "systemctl status tidb-4000"

# 检查网络连通性
tiup cluster exec tidb-cluster --command "ping pd-server-ip"

# 检查磁盘空间
tiup cluster exec tidb-cluster --command "df -h"
```

#### 5.2.2 性能问题诊断

```bash
# 检查慢查询
SELECT * FROM information_schema.CLUSTER_SLOW_QUERY
WHERE query_time > 1000000 ORDER BY query_time DESC LIMIT 10;

# 检查 Region 状态
SHOW TABLE regions FROM your_table;

# 检查集群负载
SHOW TIDB_HOT_REGIONS;
```

#### 5.2.3 数据恢复策略

```bash
# 定期备份策略
# 1. 每日全量备份
# 2. 每小时增量备份
# 3. 定期验证备份完整性

# 灾难恢复演练
# 1. 在测试环境恢复备份
# 2. 验证数据一致性
# 3. 测试应用兼容性
```

## 6. 总结

本文档详细介绍了 TiDB 的多种部署方式：

- **本地开发**：Docker 和 TiUP Playground
- **生产环境**：TiUP Cluster 和 Kubernetes
- **云平台**：TiDB Cloud 和托管服务

选择合适的部署方式取决于具体需求和环境条件。建议在开发环境充分测试后再进行生产部署。

---

**下一步学习**：学习 TiDB 的基本操作和 SQL 使用。