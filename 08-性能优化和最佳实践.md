# TiDB 性能优化和最佳实践

## 1. 性能优化概述

### 1.1 性能指标体系

#### 1.1.1 关键性能指标

**延迟指标**：
- **P95 延迟**：95% 的查询响应时间
- **P99 延迟**：99% 的查询响应时间
- **平均延迟**：所有查询的平均响应时间
- **最大延迟**：最慢查询的响应时间

**吞吐量指标**：
- **QPS**：每秒查询数
- **TPS**：每秒事务数
- **连接数**：活跃连接数
- **并发度**：同时执行的查询数

**资源利用率**：
- **CPU 使用率**：处理器使用百分比
- **内存使用率**：内存使用百分比
- **磁盘 I/O**：读写操作数量和带宽
- **网络 I/O**：网络传输带宽

```sql
-- 关键性能指标查询
SELECT
    COUNT(*) as total_queries,
    AVG(query_time) as avg_query_time,
    MAX(query_time) as max_query_time,
    PERCENTILE(query_time, 95) as p95_query_time,
    PERCENTILE(query_time, 99) as p99_query_time
FROM information_schema.CLUSTER_SLOW_QUERY
WHERE start_time >= NOW() - INTERVAL 1 HOUR;
```

#### 1.1.2 性能基线

**建立性能基线**：
```sql
-- 按小时统计性能基线
SELECT
    HOUR(start_time) as hour,
    COUNT(*) as query_count,
    AVG(query_time) as avg_time,
    SUM(CASE WHEN query_time > 1000000 THEN 1 ELSE 0 END) as slow_query_count
FROM information_schema.CLUSTER_SLOW_QUERY
WHERE start_time >= DATE_SUB(NOW(), INTERVAL 7 DAY)
GROUP BY HOUR(start_time)
ORDER BY hour;
```

**性能趋势分析**：
```sql
-- 日性能趋势
SELECT
    DATE(start_time) as date,
    COUNT(*) as total_queries,
    AVG(query_time) as avg_time,
    COUNT(DISTINCT user) as active_users
FROM information_schema.CLUSTER_SLOW_QUERY
WHERE start_time >= DATE_SUB(NOW(), INTERVAL 30 DAY)
GROUP BY DATE(start_time)
ORDER BY date;
```

### 1.2 性能优化方法论

#### 1.2.1 优化流程

**性能优化流程**：
1. **监控和识别**：通过监控识别性能问题
2. **分析和诊断**：分析问题根因
3. **制定方案**：制定优化方案
4. **实施优化**：执行优化措施
5. **验证效果**：验证优化效果
6. **持续监控**：持续监控防止问题复现

#### 1.2.2 性能诊断工具

**内置诊断工具**：
- **EXPLAIN**：查看执行计划
- **EXPLAIN ANALYZE**：详细执行分析
- **慢查询日志**：记录慢查询
- **性能监控面板**：Grafana 监控

**第三方工具**：
- **pt-query-digest**：查询分析
- **sysbench**：基准测试
- **tpcc-mysql**：TPC-C 测试

## 2. 查询优化

### 2.1 执行计划分析

#### 2.1.1 EXPLAIN 使用

**基本 EXPLAIN**：
```sql
-- 基本执行计划
EXPLAIN SELECT * FROM users WHERE id = 1;

-- 复杂查询执行计划
EXPLAIN SELECT u.username, o.order_no, o.total_amount
FROM users u
JOIN orders o ON u.id = o.user_id
WHERE u.status = 1 AND o.created_at >= '2024-01-01';
```

**EXPLAIN ANALYZE**：
```sql
-- 详细执行分析
EXPLAIN ANALYZE SELECT region, SUM(amount) as total_amount
FROM sales_data
WHERE sale_date >= '2024-01-01'
GROUP BY region;

-- 查看实际执行时间和资源使用
EXPLAIN ANALYZE SELECT *
FROM large_table
WHERE indexed_column = 'value'
ORDER BY created_at DESC
LIMIT 100;
```

#### 2.1.2 执行计划解读

**关键执行计划信息**：
- **id**：查询步骤标识符
- **estRows**：预估行数
- **task**：执行任务类型
- **access object**：访问的对象
- **operator info**：操作符信息

**常见操作符**：
- **TableReader**：表扫描
- **IndexReader**：索引扫描
- **Selection**：条件过滤
- **Projection**：列投影
- **Join**：表连接
- **Aggregation**：聚合操作

```sql
-- 分析执行计划示例
EXPLAIN FORMAT=JSON
SELECT u.username, COUNT(o.id) as order_count
FROM users u
LEFT JOIN orders o ON u.id = o.user_id
WHERE u.created_at >= '2024-01-01'
GROUP BY u.id, u.username;
```

### 2.2 索引优化

#### 2.2.1 索引设计原则

**适合创建索引的场景**：
- **WHERE 子句条件**：经常用于查询条件的列
- **JOIN 条件**：用于表连接的列
- **ORDER BY 排序**：经常排序的列
- **GROUP BY 分组**：经常分组的列

**索引类型选择**：
```sql
-- 主键索引
CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50)
);

-- 唯一索引
CREATE UNIQUE INDEX idx_email ON users(email);

-- 复合索引
CREATE INDEX idx_status_created ON users(status, created_at);

-- 部分索引
CREATE INDEX idx_active_users ON users(username) WHERE status = 1;

-- 函数索引
CREATE INDEX idx_email_lower ON users((LOWER(email)));
```

#### 2.2.2 索引优化技巧

**复合索引设计**：
```sql
-- 好的复合索引设计（遵循最左前缀原则）
CREATE INDEX idx_user_status_created ON users(status, created_at, username);

-- 覆盖索引优化
CREATE INDEX idx_order_summary ON orders(user_id, total_amount, order_date);

-- 分区表索引优化
CREATE INDEX idx_region_date ON sales_data(region, sale_date)
PARTITION BY RANGE (YEAR(sale_date));
```

**索引使用监控**：
```sql
-- 查看索引使用情况
SELECT
    table_name,
    index_name,
    rows_read,
    rows_inserted,
    rows_updated,
    rows_deleted
FROM information_schema.TIDB_INDEX_USAGE
ORDER BY rows_read DESC;

-- 查找未使用的索引
SELECT
    s.table_name,
    s.index_name,
    s.column_name,
    s.cardinality
FROM information_schema.STATISTICS s
WHERE s.table_schema = 'your_database'
AND s.index_name NOT IN (
    SELECT DISTINCT index_name
    FROM information_schema.TIDB_INDEX_USAGE
    WHERE rows_read > 0
);
```

### 2.3 查询重写

#### 2.3.1 避免常见问题

**避免全表扫描**：
```sql
-- 不推荐：全表扫描
SELECT * FROM large_table WHERE YEAR(created_at) = 2024;

-- 推荐：使用索引友好的条件
SELECT * FROM large_table
WHERE created_at >= '2024-01-01' AND created_at < '2025-01-01';
```

**避免 SELECT ***：
```sql
-- 不推荐：查询所有列
SELECT * FROM users WHERE status = 1;

-- 推荐：只查询需要的列
SELECT id, username, email FROM users WHERE status = 1;
```

**优化 LIKE 查询**：
```sql
-- 不推荐：前导通配符无法使用索引
SELECT * FROM users WHERE username LIKE '%john%';

-- 推荐：使用全文索引或前缀匹配
SELECT * FROM users WHERE username LIKE 'john%';
```

#### 2.3.2 子查询优化

**避免相关子查询**：
```sql
-- 不推荐：相关子查询
SELECT * FROM orders o
WHERE o.total_amount > (
    SELECT AVG(total_amount) FROM orders
    WHERE user_id = o.user_id
);

-- 推荐：使用 JOIN 或窗口函数
SELECT o.* FROM orders o
JOIN (
    SELECT user_id, AVG(total_amount) as avg_amount
    FROM orders
    GROUP BY user_id
) avg_orders ON o.user_id = avg_orders.user_id
WHERE o.total_amount > avg_orders.avg_amount;
```

**优化 IN 子查询**：
```sql
-- 不推荐：大型 IN 子查询
SELECT * FROM users
WHERE id IN (SELECT user_id FROM orders WHERE total_amount > 1000);

-- 推荐：使用 EXISTS 或 JOIN
SELECT u.* FROM users u
WHERE EXISTS (
    SELECT 1 FROM orders o
    WHERE o.user_id = u.id AND o.total_amount > 1000
);
```

### 2.4 连接优化

#### 2.4.1 连接顺序优化

**小表驱动大表**：
```sql
-- 推荐：小表驱动大表
SELECT * FROM large_table l
JOIN small_table s ON l.id = s.foreign_id;

-- 使用 STRAIGHT_JOIN 强制连接顺序
SELECT STRAIGHT_JOIN l.*, s.*
FROM small_table s
JOIN large_table l ON l.id = s.foreign_id;
```

#### 2.4.2 连接类型优化

**选择合适的连接类型**：
- **INNER JOIN**：只返回匹配的行
- **LEFT JOIN**：返回左表所有行
- **RIGHT JOIN**：返回右表所有行
- **FULL OUTER JOIN**：返回两个表的所有行

```sql
-- 优化后的连接查询
SELECT u.username, COUNT(o.id) as order_count
FROM users u
LEFT JOIN orders o ON u.id = o.user_id AND o.status = 'completed'
WHERE u.created_at >= '2024-01-01'
GROUP BY u.id, u.username;
```

## 3. 表设计优化

### 3.1 数据类型优化

#### 3.1.1 选择合适的数据类型

**数值类型选择**：
```sql
-- 整数类型选择
CREATE TABLE users (
    id BIGINT PRIMARY KEY,          -- 大整数，用于主键
    age TINYINT UNSIGNED,            -- 0-255，适合年龄
    status SMALLINT DEFAULT 1,      -- 0-32767，适合状态
    balance DECIMAL(10,2)            -- 精确小数，适合金额
);
```

**字符串类型选择**：
```sql
-- 字符串类型选择
CREATE TABLE products (
    id INT PRIMARY KEY,
    name VARCHAR(100),              -- 可变长度字符串
    description TEXT,                -- 长文本
    sku CHAR(20)                    -- 固定长度字符串
);
```

#### 3.1.2 避免过度设计

**避免使用过大的数据类型**：
```sql
-- 不推荐：过度使用大类型
CREATE TABLE orders (
    id VARCHAR(255) PRIMARY KEY,     -- 字符串主键效率低
    amount TEXT                     -- 金额用 TEXT 类型
);

-- 推荐：使用合适的类型
CREATE TABLE orders (
    id BIGINT PRIMARY KEY,          -- 数值主键效率高
    amount DECIMAL(12,2)            -- 金额用 DECIMAL 类型
);
```

### 3.2 分区表优化

#### 3.2.1 分区策略选择

**范围分区**：
```sql
-- 按日期范围分区
CREATE TABLE sales_data (
    id BIGINT,
    sale_date DATE NOT NULL,
    region VARCHAR(20),
    amount DECIMAL(10,2),
    PRIMARY KEY (id, sale_date)
) PARTITION BY RANGE (YEAR(sale_date)) (
    PARTITION p2023 VALUES LESS THAN (2024),
    PARTITION p2024 VALUES LESS THAN (2025),
    PARTITION pmax VALUES LESS THAN MAXVALUE
);
```

**列表分区**：
```sql
-- 按地区列表分区
CREATE TABLE sales_by_region (
    id BIGINT,
    region VARCHAR(20) NOT NULL,
    amount DECIMAL(10,2),
    PRIMARY KEY (id, region)
) PARTITION BY LIST COLUMNS(region) (
    PARTITION p_east VALUES IN ('北京', '上海', '杭州'),
    PARTITION p_west VALUES IN ('成都', '重庆', '西安'),
    PARTITION p_south VALUES IN ('广州', '深圳', '厦门')
);
```

#### 3.2.2 分区维护

**添加新分区**：
```sql
-- 添加新的年份分区
ALTER TABLE sales_data
ADD PARTITION (PARTITION p2025 VALUES LESS THAN (2026));

-- 重组分区
ALTER TABLE sales_data
REORGANIZE PARTITION pmax INTO (
    PARTITION p2025 VALUES LESS THAN (2026),
    PARTITION pmax VALUES LESS THAN MAXVALUE
);
```

**删除旧分区**：
```sql
-- 删除旧分区
ALTER TABLE sales_data
DROP PARTITION p2022;
```

### 3.3 分库分表优化

#### 3.3.1 分片策略

**水平分片**：
```sql
-- 按用户 ID 分片
CREATE TABLE users_0 LIKE users;
CREATE TABLE users_1 LIKE users;
CREATE TABLE users_2 LIKE users;

-- 分片函数
SELECT * FROM users_{user_id % 3} WHERE user_id = 123;
```

**垂直分片**：
```sql
-- 用户基础信息表
CREATE TABLE user_profile (
    id BIGINT PRIMARY KEY,
    username VARCHAR(50),
    email VARCHAR(100)
);

-- 用户扩展信息表
CREATE TABLE user_details (
    user_id BIGINT PRIMARY KEY,
    address TEXT,
    phone VARCHAR(20),
    FOREIGN KEY (user_id) REFERENCES user_profile(id)
);
```

#### 3.3.2 分片键选择

**分片键设计原则**：
- **均匀分布**：数据均匀分布到各个分片
- **查询友好**：常用查询条件包含分片键
- **避免热点**：避免单分片成为热点

**常用分片键**：
```sql
-- 用户 ID 分片
CREATE TABLE orders (
    order_id BIGINT PRIMARY KEY,
    user_id BIGINT,
    -- 其他字段
    SHARD KEY (user_id)
);

-- 时间范围分片
CREATE TABLE logs (
    id BIGINT PRIMARY KEY,
    log_time TIMESTAMP,
    -- 其他字段
    SHARD KEY (YEAR(log_time), MONTH(log_time))
);
```

## 4. 配置优化

### 4.1 TiDB 配置优化

#### 4.1.1 内存配置

**内存相关配置**：
```ini
[tidb]
# 内存限制
performance.memory-usage-control = true
performance.max-memory = "32GB"
performance.max-memory-per-query = "4GB"

# 事务内存
txn.total-size-limit = "100MB"
txn.entry-count-limit = 300000

# 查询内存限制
tidb_mem_quota_query = 1073741824  # 1GB
tidb_mem_quota_join = 268435456    # 256MB
tidb_mem_quota_sort = 134217728    # 128MB
```

**连接池配置**：
```ini
[tidb]
# 连接配置
max-connections = 1000
connection-timeout = "30s"
wait-timeout = "28800s"
interactive-timeout = "28800s"

# 执行配置
max-execution-time = 3600000
```

#### 4.1.2 查询优化配置

**优化器配置**：
```ini
[tidb]
# 优化器相关
tidb_opt_join_reorder_threshold = 10
tidb_opt_correlation_threshold = 0.9
tidb_opt_correlation_explicit_threshold = 0.7

# 统计信息
tidb_auto_analyze_ratio = 0.3
tidb_auto_analyze_start_time = "00:00 +0800"
tidb_auto_analyze_end_time = "06:00 +0800"
```

### 4.2 TiKV 配置优化

#### 4.2.1 存储配置

**Block Cache 配置**：
```ini
[tikv]
# Block Cache
storage.block-cache.capacity = "16GB"
storage.block-cache.shared = true

# Write Buffer
storage.write-buffer-size = "128MB"
storage.max-write-buffer-number = 5

# Compaction
storage.defaultcf.compression-per-level = ["no", "no", "lz4", "lz4", "lz4", "zstd", "zstd"]
storage.writecf.compression-per-level = ["no", "no", "lz4", "lz4", "lz4", "zstd", "zstd"]
```

#### 4.2.2 Raft 配置

**Raft 相关配置**：
```ini
[tikv]
# Raft 配置
raft.heartbeat-interval = "20ms"
raft.election-timeout = "1000ms"
raft.max-size-per-msg = "1MB"
raft.max-inflight-msgs = 256

# 线程池配置
[raftstore]
apply-pool-size = 4
store-pool-size = 4
```

#### 4.2.3 限制配置

**资源限制**：
```ini
[tikv]
# 资源限制
server.grpc-concurrency = 8
server.concurrency = 8
storage.scheduler-concurrency = 2048000
storage.scheduler-worker-pool-size = 8
```

### 4.3 TiFlash 配置优化

#### 4.3.1 内存和并发配置

**TiFlash 配置**：
```ini
[tiflash]
# 内存配置
profiles.default.max_memory_usage = "8GB"
profiles.default.max_memory_usage_for_all_queries = "16GB"

# 并发配置
profiles.default.max_threads = 8
profiles.default.max_concurrent_queries = 32

# 存储配置
storage.format_version = 4
storage.min_reserved_bytes_for_internal_usage = 3221225472  # 3GB
```

## 5. 应用层优化

### 5.1 连接池优化

#### 5.1.1 连接池配置

**Java HikariCP 配置**：
```java
HikariConfig config = new HikariConfig();
config.setJdbcUrl("jdbc:mysql://127.0.0.1:4000/database");
config.setUsername("username");
config.setPassword("password");
config.setMaximumPoolSize(20);
config.setMinimumIdle(5);
config.setConnectionTimeout(30000);
config.setIdleTimeout(600000);
config.setMaxLifetime(1800000);
config.setLeakDetectionThreshold(60000);
```

**Python 连接池配置**：
```python
import pymysql
from pymysql import pooling

connection_pool = pooling.MySQLConnectionPool(
    host='127.0.0.1',
    port=4000,
    user='username',
    password='password',
    database='database',
    max_connections=20,
    min_cached=5,
    max_cached=10,
    max_shared=10,
    blocking=True,
    max_usage=0,
    setsession=['SET AUTOCOMMIT=1']
)
```

#### 5.1.2 连接池监控

**连接池监控指标**：
```sql
-- 查看连接状态
SELECT
    COUNT(*) as active_connections,
    COUNT(DISTINCT user) as active_users,
    COUNT(DISTINCT host) as client_hosts
FROM information_schema.PROCESSLIST;

-- 监控连接等待
SELECT
    COUNT(*) as waiting_connections,
    AVG(time) as avg_wait_time
FROM information_schema.PROCESSLIST
WHERE state LIKE 'waiting%';
```

### 5.2 批量操作优化

#### 5.2.1 批量插入

**批量插入优化**：
```sql
-- 单条插入（不推荐）
INSERT INTO orders (user_id, order_no, total_amount) VALUES (1, 'ORDER001', 100.00);
INSERT INTO orders (user_id, order_no, total_amount) VALUES (2, 'ORDER002', 200.00);

-- 批量插入（推荐）
INSERT INTO orders (user_id, order_no, total_amount) VALUES
(1, 'ORDER001', 100.00),
(2, 'ORDER002', 200.00),
(3, 'ORDER003', 300.00);
```

**LOAD DATA 批量导入**：
```sql
-- 使用 LOAD DATA 导入大量数据
LOAD DATA LOCAL INFILE '/path/to/orders.csv'
INTO TABLE orders
FIELDS TERMINATED BY ','
LINES TERMINATED BY '\n'
IGNORE 1 ROWS
(user_id, order_no, total_amount, order_date);
```

#### 5.2.2 批量更新

**批量更新优化**：
```sql
-- 单条更新（不推荐）
UPDATE products SET stock = stock - 1 WHERE id = 1;
UPDATE products SET stock = stock - 1 WHERE id = 2;

-- CASE WHEN 批量更新
UPDATE products
SET stock = CASE id
    WHEN 1 THEN stock - 1
    WHEN 2 THEN stock - 1
    ELSE stock
END
WHERE id IN (1, 2);
```

### 5.3 缓存优化

#### 5.3.1 查询缓存

**应用层缓存**：
```java
// Redis 缓存示例
public class ProductService {
    private RedisTemplate<String, Object> redisTemplate;

    public Product getProductById(Long id) {
        String key = "product:" + id;

        // 先查缓存
        Product product = (Product) redisTemplate.opsForValue().get(key);
        if (product != null) {
            return product;
        }

        // 缓存未命中，查数据库
        product = productMapper.selectById(id);
        if (product != null) {
            // 放入缓存，设置过期时间
            redisTemplate.opsForValue().set(key, product, 30, TimeUnit.MINUTES);
        }

        return product;
    }
}
```

#### 5.3.2 缓存策略

**缓存更新策略**：
```java
// 更新产品时更新缓存
@Transactional
public void updateProduct(Product product) {
    // 更新数据库
    productMapper.updateById(product);

    // 删除或更新缓存
    String key = "product:" + product.getId();
    redisTemplate.delete(key);
    // 或者更新缓存
    // redisTemplate.opsForValue().set(key, product, 30, TimeUnit.MINUTES);
}
```

## 6. 事务优化

### 6.1 事务管理

#### 6.1.1 事务大小优化

**控制事务大小**：
```sql
-- 大事务（不推荐）
BEGIN;
INSERT INTO large_table (...) VALUES (...);  -- 10万条数据
COMMIT;

-- 小事务（推荐）
BEGIN;
INSERT INTO large_table (...) VALUES (...);  -- 1000条数据
COMMIT;
```

**批量事务处理**：
```java
// Java 批量事务处理示例
public void batchInsert(List<Order> orders) {
    int batchSize = 1000;
    for (int i = 0; i < orders.size(); i += batchSize) {
        List<Order> batch = orders.subList(i, Math.min(i + batchSize, orders.size()));
        transactionTemplate.execute(status -> {
            orderMapper.batchInsert(batch);
            return null;
        });
    }
}
```

#### 6.1.2 事务隔离级别

**选择合适的隔离级别**：
```sql
-- 读已提交（默认）
SET SESSION TRANSACTION ISOLATION LEVEL READ COMMITTED;

-- 可重复读（需要一致性时）
SET SESSION TRANSACTION ISOLATION LEVEL REPEATABLE READ;

-- 只读事务优化
BEGIN TRANSACTION READ ONLY;
SELECT * FROM products WHERE category = 'electronics';
COMMIT;
```

### 6.2 锁优化

#### 6.2.1 避免锁等待

**减少锁持有时间**：
```java
// 短事务示例
@Transactional(timeout = 5)  // 5秒超时
public void updateOrder(Order order) {
    // 快速更新，减少锁持有时间
    orderMapper.updateById(order);
}
```

**锁超时设置**：
```sql
-- 设置锁等待超时
SET SESSION innodb_lock_wait_timeout = 50;  -- 50秒

-- 监控锁等待
SELECT * FROM information_schema.CLUSTER_LOCK_WAITS;
```

#### 6.2.2 死锁预防和处理

**死锁预防策略**：
1. **按固定顺序访问资源**
2. **减少事务大小**
3. **设置合理的锁超时**
4. **重试机制**

**死锁监控**：
```sql
-- 查看死锁信息
SELECT * FROM information_schema.CLUSTER_DEADLOCKS
WHERE deadlock_time >= NOW() - INTERVAL 1 HOUR;
```

## 7. 监控和分析

### 7.1 性能监控

#### 7.1.1 慢查询分析

**慢查询监控**：
```sql
-- 按小时统计慢查询
SELECT
    HOUR(start_time) as hour,
    COUNT(*) as slow_count,
    AVG(query_time) as avg_time,
    MAX(query_time) as max_time
FROM information_schema.CLUSTER_SLOW_QUERY
WHERE start_time >= CURDATE()
GROUP BY HOUR(start_time)
ORDER BY hour;
```

**慢查询模式分析**：
```sql
-- 分析常见慢查询模式
SELECT
    digest_text,
    COUNT(*) as execution_count,
    AVG(query_time) as avg_time,
    SUM(query_time) as total_time
FROM information_schema.CLUSTER_SLOW_QUERY
WHERE start_time >= NOW() - INTERVAL 1 DAY
GROUP BY digest_text
ORDER BY total_time DESC
LIMIT 10;
```

#### 7.1.2 资源监控

**CPU 监控**：
```sql
-- CPU 使用率监控
SELECT
    node_id,
    cpu_usage,
    memory_usage,
    io_usage
FROM information_schema.TIDB_NODE_METRICS
WHERE timestamp >= NOW() - INTERVAL 1 HOUR;
```

**内存监控**：
```sql
-- 内存使用监控
SELECT
    node_id,
    total_memory,
    used_memory,
    available_memory,
    memory_usage_percent
FROM information_schema.TIDB_NODE_MEMORY
ORDER BY memory_usage_percent DESC;
```

### 7.2 性能分析工具

#### 7.2.1 执行计划分析

**使用 EXPLAIN ANALYZE**：
```sql
-- 分析执行计划
EXPLAIN ANALYZE
SELECT u.username, COUNT(o.id) as order_count
FROM users u
LEFT JOIN orders o ON u.id = o.user_id
WHERE u.created_at >= '2024-01-01'
GROUP BY u.id, u.username;
```

#### 7.2.2 性能分析脚本

**性能分析脚本**：
```bash
#!/bin/bash

# TiDB 性能分析脚本
MYSQL_USER="root"
MYSQL_HOST="127.0.0.1"
MYSQL_PORT="4000"
OUTPUT_FILE="/tmp/performance_analysis_$(date +%Y%m%d_%H%M%S).txt"

echo "TiDB 性能分析报告" > $OUTPUT_FILE
echo "分析时间: $(date)" >> $OUTPUT_FILE
echo "================================" >> $OUTPUT_FILE

# 慢查询统计
echo "慢查询统计:" >> $OUTPUT_FILE
mysql -h$MYSQL_HOST -P$MYSQL_PORT -u$MYSQL_USER -e "
    SELECT
        COUNT(*) as total_slow_queries,
        AVG(query_time) as avg_time,
        MAX(query_time) as max_time
    FROM information_schema.CLUSTER_SLOW_QUERY
    WHERE start_time >= NOW() - INTERVAL 1 HOUR;
" >> $OUTPUT_FILE

# 热点表
echo -e "\n热点表访问统计:" >> $OUTPUT_FILE
mysql -h$MYSQL_HOST -P$MYSQL_PORT -u$MYSQL_USER -e "
    SELECT
        table_name,
        SUM(read_count) as total_reads,
        SUM(write_count) as total_writes
    FROM information_schema.TABLE_ACCESS_STATS
    GROUP BY table_name
    ORDER BY total_reads + total_writes DESC
    LIMIT 10;
" >> $OUTPUT_FILE

# 连接数
echo -e "\n连接数统计:" >> $OUTPUT_FILE
mysql -h$MYSQL_HOST -P$MYSQL_PORT -u$MYSQL_USER -e "
    SELECT
        COUNT(*) as active_connections,
        COUNT(DISTINCT user) as active_users
    FROM information_schema.PROCESSLIST;
" >> $OUTPUT_FILE

echo "分析完成，报告保存至: $OUTPUT_FILE"
```

## 8. 最佳实践总结

### 8.1 设计阶段最佳实践

#### 8.1.1 数据库设计

**Schema 设计原则**：
- 选择合适的数据类型
- 设计合理的索引
- 避免过度规范化
- 考虑查询模式

**索引设计原则**：
- 为常用查询条件创建索引
- 遵循最左前缀原则
- 避免过多索引
- 定期维护索引

#### 8.1.2 架构设计

**读写分离**：
```java
// 读写分离配置示例
@Configuration
public class DataSourceConfig {

    @Bean
    @ConfigurationProperties(prefix = "spring.datasource.master")
    public DataSource masterDataSource() {
        return DataSourceBuilder.create().build();
    }

    @Bean
    @ConfigurationProperties(prefix = "spring.datasource.slave")
    public DataSource slaveDataSource() {
        return DataSourceBuilder.create().build();
    }

    @Bean
    public DataSource routingDataSource() {
        DynamicDataSource routingDataSource = new DynamicDataSource();
        Map<Object, Object> dataSourceMap = new HashMap<>();
        dataSourceMap.put("master", masterDataSource());
        dataSourceMap.put("slave", slaveDataSource());
        routingDataSource.setTargetDataSources(dataSourceMap);
        routingDataSource.setDefaultTargetDataSource(masterDataSource());
        return routingDataSource;
    }
}
```

### 8.2 开发阶段最佳实践

#### 8.2.1 代码优化

**SQL 编写规范**：
```java
// 好的 SQL 编写示例
@Select("SELECT id, username, email FROM users WHERE status = #{status} AND created_at >= #{startDate}")
List<User> findActiveUsers(@Param("status") Integer status, @Param("startDate") LocalDate startDate);

// 避免 SELECT *
@Select("SELECT id, username, email FROM users WHERE id = #{id}")
User findById(@Param("id") Long id);
```

**连接池配置**：
```yaml
# application.yml
spring:
  datasource:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000
```

#### 8.2.2 测试和优化

**性能测试**：
```java
@Test
public void testQueryPerformance() {
    // 执行查询并测量时间
    long startTime = System.currentTimeMillis();
    List<Order> orders = orderMapper.findByUserId(123L);
    long endTime = System.currentTimeMillis();

    log.info("查询耗时: {} ms", endTime - startTime);

    // 验证查询计划
    List<ExecutionPlan> plans = executionPlanMapper.getExecutionPlan("SELECT * FROM orders WHERE user_id = 123");
    assertTrue(plans.stream().anyMatch(p -> p.getOperator().contains("IndexReader")));
}
```

### 8.3 运维阶段最佳实践

#### 8.3.1 监控和告警

**关键监控指标**：
- 查询响应时间
- QPS/TPS
- 错误率
- 资源使用率

**告警配置**：
```yaml
# prometheus.yml
groups:
  - name: tidb_alerts
    rules:
      - alert: HighQueryLatency
        expr: histogram_quantile(0.95, rate(tidb_executor_statement_duration_seconds_bucket[5m])) > 2
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "查询延迟过高"
          description: "95% 查询延迟超过 2 秒"
```

#### 8.3.2 容量规划

**扩容决策流程**：
1. 监控资源使用率
2. 预测未来增长
3. 制定扩容计划
4. 执行扩容操作
5. 验证扩容效果

## 9. 性能优化检查清单

### 9.1 日常优化检查

**数据库配置**：
- [ ] 检查连接池配置
- [ ] 优化内存设置
- [ ] 检查慢查询日志
- [ ] 分析执行计划

**索引优化**：
- [ ] 检查未使用的索引
- [ ] 添加缺失的索引
- [ ] 优化复合索引
- [ ] 更新统计信息

**查询优化**：
- [ ] 避免全表扫描
- [ ] 优化 JOIN 查询
- [ ] 减少子查询
- [ ] 使用合适的数据类型

### 9.2 定期维护任务

**日常维护（每日）**：
- 监控慢查询
- 检查资源使用率
- 验证备份状态

**每周维护**：
- 分析性能趋势
- 更新统计信息
- 检查索引使用情况

**每月维护**：
- 容量规划评估
- 性能基准测试
- 优化配置调整

### 9.3 性能优化模板

**优化报告模板**：
```
TiDB 性能优化报告
======================

分析时间: 2024-01-15 10:00:00
集群名称: tidb-cluster

1. 性能问题分析
   - 问题1: 查询响应时间过长
     表现: P95延迟从 100ms 增加到 500ms
     影响: 用户体验下降
     根因: 缺少合适索引

2. 优化措施
   - 添加复合索引: idx_user_status_created
   - 优化查询: 重写慢查询 SQL
   - 调整配置: 增加连接池大小

3. 优化效果
   - P95延迟: 从 500ms 降低到 80ms
   - QPS: 从 1000 提升到 1500
   - CPU使用率: 从 80% 降低到 60%

4. 建议措施
   - 定期监控性能指标
   - 建立性能基线
   - 制定优化计划
```

## 10. 总结

TiDB 性能优化是一个系统工程，需要从多个层面进行：

- **查询层面**：优化 SQL 语句和执行计划
- **索引层面**：设计合理的索引结构
- **表设计层面**：选择合适的数据类型和分区策略
- **配置层面**：优化数据库参数配置
- **应用层面**：优化连接池和缓存策略
- **运维层面**：建立完善的监控和告警体系

通过系统性的性能优化，可以显著提升 TiDB 集群的性能和稳定性，为业务提供更好的支撑。

---

**下一步学习**：学习 TiDB 的故障排查和常见问题。