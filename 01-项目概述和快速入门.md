# TiDB 项目概述和快速入门指南

## 1. 项目简介

### 1.1 什么是 TiDB？

TiDB 是一个开源的、云原生的分布式 SQL 数据库，专为高可用性、水平垂直扩展、强一致性和高性能而设计。TiDB 具有以下核心特点：

- **分布式事务**：使用两阶段提交协议确保 ACID 合规性
- **水平扩展**：通过添加节点实现无缝扩展
- **高可用性**：内置 Raft 共识协议提供自动化故障转移
- **HTAP 能力**：混合事务/分析处理能力
- **MySQL 兼容**：兼容 MySQL 8.0 协议
- **云原生**：支持 Kubernetes 和多云部署

### 1.2 项目结构分析

```
tidb-learning/
├── tidb/                    # TiDB 源代码目录
│   ├── cmd/                 # 命令行工具
│   ├── pkg/                 # 核心包和库
│   ├── docs/                # 文档
│   ├── tests/               # 测试文件
│   ├── tools/               # 工具集
│   ├── lightning/           # 数据导入工具
│   ├── dumpling/            # 数据导出工具
│   └── br/                  # 备份恢复工具
```

## 2. 核心特性详解

### 2.1 分布式架构优势

**计算存储分离**
- TiDB Server：SQL 层，负责 SQL 解析和执行
- TiKV：存储层，分布式行存储引擎
- TiFlash：分析引擎，列式存储
- PD (Placement Driver)：集群调度器

**高可用设计**
- Raft 协议确保数据一致性
- 多副本存储策略
- 自动故障检测和恢复

### 2.2 HTAP 混合能力

**实时数据分析**
- 行存储 TiKV 负责事务处理
- 列存储 TiFlash 负责分析查询
- Multi-Raft Learner 协议同步数据

## 3. 快速入门指南

### 3.1 本地环境搭建

#### 方式一：Docker 快速启动

```bash
# 拉取 TiDB 镜像
docker pull pingcap/tidb:v7.1.0

# 启动 TiDB 容器
docker run -p 4000:4000 pingcap/tidb:v7.1.0

# 连接 TiDB
mysql -h 127.0.0.1 -P 4000 -u root
```

#### 方式二：TiUP 部署

```bash
# 安装 TiUP
curl --proto '=https' --tlsv1.2 -sSf https://tiup-mirrors.pingcap.com/install.sh | sh

# 安装 TiDB 组件
tiup install tidb

# 启动本地集群
tiup playground
```

### 3.2 基础操作演示

#### 3.2.1 连接和基本查询

```sql
-- 连接 TiDB
mysql -h 127.0.0.1 -P 4000 -u root

-- 创建数据库
CREATE DATABASE test_db;
USE test_db;

-- 创建表
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL,
    email VARCHAR(100) UNIQUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 插入数据
INSERT INTO users (name, email) VALUES
('张三', 'zhangsan@example.com'),
('李四', 'lisi@example.com'),
('王五', 'wangwu@example.com');

-- 查询数据
SELECT * FROM users;
```

#### 3.2.2 分布式特性体验

```sql
-- 创建分片表（TiDB 自动分片）
CREATE TABLE orders (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id INT,
    amount DECIMAL(10,2),
    order_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_id (user_id)
) SHARD_ROW_ID_BITS = 4 PRE_SPLIT_REGIONS = 3;

-- 插入大量数据测试分布式能力
INSERT INTO orders (user_id, amount)
SELECT FLOOR(RAND() * 1000) + 1, ROUND(RAND() * 1000, 2)
FROM (
    SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5
) a, (
    SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5
) b, (
    SELECT 1 UNION SELECT 2 UNION SELECT 3 UNION SELECT 4 UNION SELECT 5
) c;

-- 查看数据分布
SHOW TABLE regions FROM orders;
```

#### 3.2.3 HTAP 功能测试

```sql
-- 创建分析表并启用 TiFlash 副本
CREATE TABLE sales_data (
    id BIGINT,
    product_id INT,
    quantity INT,
    sale_date DATE,
    region VARCHAR(20),
    INDEX idx_region_date (region, sale_date)
);

-- 设置 TiFlash 副本
ALTER TABLE sales_data SET TIFLASH REPLICA 1;

-- 插入测试数据
INSERT INTO sales_data VALUES
(1, 101, 5, '2024-01-01', '北京'),
(2, 102, 3, '2024-01-01', '上海'),
(3, 101, 2, '2024-01-02', '广州'),
(4, 103, 8, '2024-01-02', '深圳'),
(5, 102, 4, '2024-01-03', '北京');

-- 执行分析查询（自动使用 TiFlash）
SELECT region, SUM(quantity) as total_sales
FROM sales_data
GROUP BY region
ORDER BY total_sales DESC;

-- 检查查询执行计划
EXPLAIN SELECT region, SUM(quantity) as total_sales
FROM sales_data
GROUP BY region;
```

### 3.3 常用管理操作

#### 3.3.1 集群状态查看

```sql
-- 查看集群信息
SHOW STATUS LIKE 'TiDB_version%';
SHOW VARIABLES LIKE '%version%';

-- 查看节点状态
SHOW DDL JOBS;
SHOW TABLE REGIONS;

-- 查看存储引擎信息
SHOW STORAGE ENGINES;
```

#### 3.3.2 性能监控

```sql
-- 查看慢查询
SELECT query_time, digest_text
FROM information_schema.CLUSTER_SLOW_QUERY
WHERE query_time > 1000000  -- 1秒
ORDER BY query_time DESC
LIMIT 10;

-- 查看集群负载
SELECT * FROM information_schema.TIDB_HOT_REGIONS
LIMIT 10;
```

## 4. 开发环境配置

### 4.1 Go 环境设置

```bash
# 安装 Go 1.19+
wget https://golang.org/dl/go1.19.linux-amd64.tar.gz
sudo tar -C /usr/local -xzf go1.19.linux-amd64.tar.gz

# 设置环境变量
export PATH=$PATH:/usr/local/go/bin
export GOPATH=$HOME/go
```

### 4.2 编译 TiDB 源码

```bash
# 克隆项目
cd /Users/lishizheng/Desktop/Code/tidb-learning
git clone https://github.com/pingcap/tidb.git

# 编译 TiDB
cd tidb
make

# 运行测试
make test

# 启动开发服务器
./bin/tidb-server --store=tikv --path="127.0.0.1:2379"
```

### 4.3 IDE 配置（VS Code）

安装以下扩展：
- Go 语言支持
- MySQL 客户端
- Docker
- GitLens

配置调试设置：
```json
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch TiDB",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/tidb-server",
            "args": [
                "--store=tikv",
                "--path=127.0.0.1:2379",
                "--port=4000"
            ]
        }
    ]
}
```

## 5. 下一步学习路径

1. **基础概念**：理解 TiDB 的分布式架构和核心组件
2. **安装部署**：掌握不同环境下的部署方法
3. **SQL 使用**：学习 TiDB 的 SQL 特性和最佳实践
4. **运维监控**：了解集群管理和监控工具
5. **性能优化**：掌握性能调优和故障排查
6. **实战案例**：通过实际项目应用 TiDB

## 6. 学习资源

- [官方文档](https://docs.pingcap.com/tidb/stable/)
- [在线 Playground](https://play.tidbcloud.com/)
- [GitHub 仓库](https://github.com/pingcap/tidb)
- [社区论坛](https://asktug.com/)
- [技术博客](https://www.pingcap.com/blog/)

---

**提示**：建议按照文档顺序学习，每个概念都进行实践操作以加深理解。