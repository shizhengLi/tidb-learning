# TiDB 分布式特性详解

## 1. 分布式存储架构

### 1.1 数据分片原理

#### 1.1.1 Region 概念

**Region 是 TiDB 的基本数据分片单元**：

- **默认大小**：96MB
- **自动分裂**：超过阈值时自动分裂
- **范围连续**：每个 Region 包含连续的 Key 范围
- **Raft 组**：每个 Region 形成一个独立的 Raft 组

```sql
-- 查看表的 Region 信息
SHOW TABLE regions FROM your_table;

-- 查看 Region 详细信息
SHOW REGIONS WHERE db_name = 'your_database';

-- 查看 Region 统计信息
SELECT
    TABLE_NAME,
    REGION_COUNT,
    REGION_SIZE,
    APPROXIMATE_KEYS
FROM information_schema.TIDB_TABLES
WHERE TABLE_SCHEMA = 'your_database';
```

#### 1.1.2 数据分布策略

**Key-Value 映射规则**：
```
Key:   tableID + indexID + rowID
Value: encodedRowData
```

**分片键选择策略**：
- **主键分片**：默认使用主键进行分片
- **索引分片**：索引数据独立分片
- **哈希分片**：避免热点问题

```sql
-- 创建表时设置分片参数
CREATE TABLE orders (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT,
    order_no VARCHAR(32),
    created_at TIMESTAMP,
    INDEX idx_user_id (user_id)
) SHARD_ROW_ID_BITS = 4 PRE_SPLIT_REGIONS = 3;

-- 查看分片信息
SHOW CREATE TABLE orders;
```

### 1.2 Raft 一致性协议

#### 1.2.1 Raft 基础概念

**Raft 组构成**：
- **Leader**：处理所有读写请求
- **Follower**：同步 Leader 数据
- **Candidate**：选举期间的候选状态

**选举过程**：
1. Follower 超时未收到 Leader 心跳
2. 变为 Candidate，发起选举
3. 获得多数票后成为 Leader
4. 定期发送心跳维持领导地位

```sql
-- 查看 Region Leader 信息
SELECT
    region_id,
    leader_id,
    peer_id,
    role
FROM information_schema.TIDB_REGION_STATUS
WHERE table_name = 'your_table';
```

#### 1.2.2 数据一致性保证

**写操作流程**：
1. 客户端请求到达 TiDB Server
2. TiDB 定位到目标 Region 的 Leader
3. Leader 将写入操作应用到 Raft 日志
4. 复制到多数 Follower 确认
5. 返回成功响应

**读操作选项**：
- **Leader Read**：只从 Leader 读取，保证强一致性
- **Follower Read**：可以从 Follower 读取，性能更好但可能延迟

```sql
-- 设置读一致性级别
SET GLOBAL tidb_replica_read = 'leader';
SET GLOBAL tidb_replica_read = 'follower';
SET GLOBAL tidb_replica_read = 'leader-and-follower';
```

### 1.3 多副本机制

#### 1.3.1 副本配置

**默认副本数**：3 副本
**副本放置策略**：
- 跨服务器机架
- 跨可用区
- 跨数据中心

```sql
-- 查看副本信息
SELECT
    region_id,
    store_id,
    peer_id,
    is_leader,
    role
FROM information_schema.TIDB_REGION_PEERS
WHERE table_name = 'your_table';

-- 设置副本数量
ALTER TABLE your_table REPLICA 3;
```

#### 1.3.2 副本同步机制

**日志复制**：
- Leader 将操作日志同步到所有 Follower
- 需要获得多数节点确认才算提交成功
- 保证数据的强一致性

**故障恢复**：
- 自动检测 Leader 故障
- 重新选举新 Leader
- 数据自动同步和修复

```sql
-- 查看副本健康状态
SELECT
    store_id,
    region_count,
    leader_count,
    region_score
FROM information_schema.TIDB_STORE_STATUS;
```

## 2. 分布式事务

### 2.1 全局时间戳

#### 2.1.1 TSO (Timestamp Oracle)

**TSO 作用**：
- 为所有事务分配全局唯一时间戳
- 保证事务的线性一致性
- 实现 MVCC 机制

**时间戳组成**：
```
TSO = 物理时间部分 + 逻辑时间部分
     45位        + 18位
```

```sql
-- 查看当前时间戳
SELECT @@tidb_current_ts;

-- 查看时间戳信息
SHOW VARIABLES LIKE '%timestamp%';
```

#### 2.1.2 事务生命周期

**事务时间戳分配**：
- **start_ts**：事务开始时间戳
- **commit_ts**：事务提交时间戳
- **min_commit_ts**：最小提交时间戳

**MVCC 实现**：
- 每个数据版本都有创建和删除时间戳
- 根据事务 start_ts 选择可见版本
- 历史版本保留用于回滚和一致性读

```sql
-- 查看事务信息
SELECT * FROM information_schema.CLUSTER_TRANSACTIONS;

-- 查看事务状态
SHOW PROCESSLIST;
```

### 2.2 两阶段提交 (2PC)

#### 2.2.1 2PC 协议详解

**第一阶段 - Prewrite**：
1. 事务协调器在所有参与者节点预执行
2. 获取所有相关数据的锁
3. 写入数据但不提交
4. 等待所有参与者确认准备完成

**第二阶段 - Commit**：
1. 协调器根据 Prewrite 结果决定提交或回滚
2. 通知所有参与者执行最终决定
3. 释放持有的锁
4. 返回最终结果给客户端

```sql
-- 监控事务执行
SELECT
    transaction_id,
    start_ts,
    state,
    duration,
    retry_count
FROM information_schema.CLUSTER_TRANSACTIONS
WHERE state != 'committed';
```

#### 2.2.2 分布式锁机制

**锁类型**：
- **行锁**：锁定单行数据
- **范围锁**：锁定键值范围
- **意向锁**：提高锁冲突检测效率

**锁冲突处理**：
- **等待机制**：等待锁释放
- **超时机制**：锁等待超时后回滚
- **死锁检测**：自动检测和处理死锁

```sql
-- 查看锁等待信息
SELECT * FROM information_schema.CLUSTER_LOCK_WAITS;

-- 查看死锁信息
SELECT * FROM information_schema.CLUSTER_DEADLOCKS;
```

### 2.3 事务隔离级别

#### 2.3.1 支持的隔离级别

**Read Committed (RC)**：
- 默认隔离级别
- 只能看到已提交的数据
- 可能出现不可重复读

**Repeatable Read (RR)**：
- 基于快照的隔离级别
- 事务内看到一致的数据视图
- 避免不可重复读和幻读

```sql
-- 设置隔离级别
SET SESSION TRANSACTION ISOLATION LEVEL READ COMMITTED;
SET SESSION TRANSACTION ISOLATION LEVEL REPEATABLE READ;

-- 查看当前隔离级别
SELECT @@transaction_isolation;
```

#### 2.3.2 快照隔离实现

**快照读取机制**：
- 事务开始时获取 start_ts
- 读取数据时选择该时间戳的版本
- 保证事务内看到一致的数据视图

**版本清理机制**：
- 定期清理过期版本
- 保留必要的版本用于回滚
- 避免无限增长

```sql
-- 使用快照读取
BEGIN;
SELECT * FROM accounts WHERE id = 1;  -- 获取快照
-- 其他事务修改数据
SELECT * FROM accounts WHERE id = 1;  -- 仍然看到相同数据
COMMIT;
```

## 3. 分布式调度

### 3.1 PD (Placement Driver)

#### 3.1.1 PD 核心功能

**元数据管理**：
- 集群拓扑信息
- 表和索引的元数据
- Region 位置信息

**调度决策**：
- Region 位置调度
- Leader 选举和迁移
- 负载均衡

**全局服务**：
- 时间戳分配器 (TSO)
- 唯一 ID 生成器
- 集群配置管理

```sql
-- 查看 PD 状态
SHOW TIDB_CLUSTER_CONFIG;

-- 查看 PD 调度信息
SELECT * FROM information_schema.TIDB_SCHEDULERS;
```

#### 3.1.2 PD 集群架构

**PD 集群特性**：
- 多 PD 节点组成 Raft 组
- 自动故障转移
- 一致的元数据视图

**高可用机制**：
- Raft 协议保证一致性
- Leader 自动选举
- 数据多副本存储

```sql
-- 查看 PD 节点信息
SELECT * FROM information_schema.TIDB_CLUSTER_INFO
WHERE TYPE = 'pd';

-- 查看集群健康状态
SHOW TIDB_CLUSTER_HEALTH;
```

### 3.2 调度策略

#### 3.2.1 Region 调度

**Region 均衡**：
- 根据 Store 负载调度 Region
- 避免单节点负载过高
- 保持集群整体均衡

**热点调度**：
- 检测热点 Region
- 将热点 Region 分散到不同节点
- 提高整体吞吐量

```sql
-- 查看热点 Region
SELECT
    table_name,
    region_id,
    read_bytes,
    write_bytes,
    written_keys
FROM information_schema.TIDB_HOT_REGIONS
ORDER BY write_bytes DESC
LIMIT 10;

-- 查看 Store 负载
SELECT
    store_id,
    capacity,
    available,
    leader_count,
    region_count,
    region_score
FROM information_schema.TIDB_STORE_STATUS;
```

#### 3.2.2 Leader 调度

**Leader 均衡**：
- Leader 均匀分布到不同节点
- 减少单节点读压力
- 提高读取并发能力

**读写分离**：
- Leader 处理写请求
- Follower 处理读请求
- 减轻 Leader 负载

```sql
-- 查看 Leader 分布
SELECT
    store_id,
    leader_count,
    leader_region_size
FROM information_schema.TIDB_STORE_STATUS
ORDER BY leader_count DESC;
```

### 3.3 负载均衡

#### 3.3.1 自动负载均衡

**负载指标**：
- **Region 数量**：每个 Store 的 Region 数
- **Leader 数量**：每个 Store 的 Leader 数
- **数据大小**：每个 Store 的数据量
- **读写流量**：每个 Store 的 I/O 流量

**均衡策略**：
- 定期检查负载状态
- 自动调度 Region 和 Leader
- 优先处理热点问题

```sql
-- 查看负载均衡状态
SELECT
    store_id,
    region_count / AVG(region_count) OVER() as region_ratio,
    leader_count / AVG(leader_count) OVER() as leader_ratio
FROM information_schema.TIDB_STORE_STATUS;
```

#### 3.3.2 手动调度干预

**手动调度命令**：
```bash
# 使用 pd-ctl 工具手动调度
pd-ctl -u http://pd-ip:2379 scheduler add
pd-ctl -u http://pd-ip:2379 region operator add
pd-ctl -u http://pd-ip:2379 leader transfer
```

**调度策略配置**：
```sql
-- 查看调度配置
SHOW CONFIG WHERE name LIKE '%scheduler%';

-- 修改调度参数（需要管理员权限）
SET GLOBAL tidb_scheduler_limit = 100;
```

## 4. 数据分布和分片

### 4.1 分片策略

#### 4.1.1 自动分片

**自动分片触发条件**：
- Region 大小超过 96MB
- Key 数量超过阈值
- 写入负载过高

**分片过程**：
1. PD 检测到 Region 需要分裂
2. 选择分裂点（中间位置）
3. 创建新的 Region
4. 更新路由信息

```sql
-- 创建表时配置分片参数
CREATE TABLE orders (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT,
    order_no VARCHAR(32),
    created_at TIMESTAMP
) SHARD_ROW_ID_BITS = 4 PRE_SPLIT_REGIONS = 3;

-- 查看分片配置
SHOW CREATE TABLE orders;
```

#### 4.1.2 预分片

**预分片优势**：
- 避免初始写入热点
- 提高并发写入能力
- 减少运行时分裂开销

**预分片配置**：
- `SHARD_ROW_ID_BITS`：分片位数
- `PRE_SPLIT_REGIONS`：预分片数量
- `SPLIT_REGION`：手动分片命令

```sql
-- 手动分片
SPLIT TABLE orders BETWEEN (0) AND (1000000) REGIONS 5;

-- 查看分片结果
SHOW TABLE regions FROM orders;
```

### 4.2 热点问题处理

#### 4.2.1 热点识别

**写入热点**：
- 自增主键导致末尾 Region 热点
- 时间序列数据的时间热点
- 特定业务模式的访问热点

**读取热点**：
- 缓存失效导致的并发读取
- 热门数据的集中访问
- 统计查询的并发执行

```sql
-- 监控热点写入
SELECT
    table_name,
    region_id,
    written_keys,
    written_bytes,
    written_query_count
FROM information_schema.TIDB_HOT_REGIONS
ORDER BY written_keys DESC
LIMIT 10;

-- 监控热点读取
SELECT
    table_name,
    region_id,
    read_bytes,
    read_keys,
    read_query_count
FROM information_schema.TIDB_HOT_REGIONS
ORDER BY read_bytes DESC
LIMIT 10;
```

#### 4.2.2 热点解决方案

**解决方案**：
1. **主键设计**：使用分布式 ID 或随机主键
2. **分表分库**：将热点数据分散到不同表
3. **缓存策略**：减少数据库直接访问
4. **读写分离**：分散读取压力

**最佳实践**：
```sql
-- 使用 UUID 或雪花算法生成主键
CREATE TABLE users (
    id CHAR(36) PRIMARY KEY,  -- UUID
    username VARCHAR(50),
    created_at TIMESTAMP
);

-- 使用 SHARD_ROW_ID_BITS 避免热点
CREATE TABLE orders (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id BIGINT,
    order_no VARCHAR(32)
) SHARD_ROW_ID_BITS = 4;

-- 分表策略处理大表
CREATE TABLE orders_2024_01 PARTITION BY RANGE (TO_DAYS(created_at)) (
    PARTITION p20240101 VALUES LESS THAN (TO_DAYS('2024-01-02')),
    PARTITION p20240102 VALUES LESS THAN (TO_DAYS('2024-01-03')),
    PARTITION p20240103 VALUES LESS THAN (TO_DAYS('2024-01-04'))
);
```

## 5. 弹性扩展

### 5.1 水平扩展

#### 5.1.1 节点扩容

**扩容流程**：
1. 部署新的 TiKV 节点
2. 新节点加入集群
3. PD 自动调度 Region 到新节点
4. 数据自动平衡分布

**扩容监控**：
- 监控数据迁移进度
- 观察负载均衡状态
- 检查集群整体性能

```bash
# 使用 TiUP 添加 TiKV 节点
tiup cluster scale-out tidb-cluster scale-out.yaml

# 监控扩容进度
tiup cluster display tidb-cluster
```

#### 5.1.2 缩容操作

**缩容流程**：
1. 标记要下线的节点
2. PD 将数据迁移到其他节点
3. 数据迁移完成后移除节点
4. 更新集群配置

**注意事项**：
- 确保有足够容量接收迁移数据
- 监控缩容过程中的性能影响
- 备份重要数据

```bash
# 缩容节点
tiup cluster scale-in tidb-cluster -N 10.0.1.10:20160

# 查看缩容进度
tiup cluster display tidb-cluster
```

### 5.2 在线扩容

#### 5.2.1 零停机扩容

**在线扩容特性**：
- 业务无感知
- 数据自动迁移
- 负载自动均衡
- 服务不中断

**扩容策略**：
- 逐步添加节点
- 控制迁移速度
- 监控系统负载
- 确保数据一致性

```sql
-- 监控扩容状态
SELECT
    store_id,
    region_count,
    leader_count,
    region_size,
    leader_size,
    available
FROM information_schema.TIDB_STORE_STATUS;

-- 查看数据迁移进度
SELECT
    table_name,
    region_id,
    is_merging,
    merge_state
FROM information_schema.TIDB_REGION_STATUS
WHERE is_merging = true;
```

#### 5.2.2 容量规划

**规划考虑因素**：
- **数据增长率**：预期数据增长速度
- **访问模式**：读写比例和热点情况
- **性能要求**：响应时间和吞吐量要求
- **可用性要求**：副本数和容错能力

**容量计算**：
```
总容量 = 数据量 × (1 + 增长率) × 副本数 + 预留空间
节点数量 = 总容量 / 单节点容量 + 冗余节点
```

```sql
-- 查看集群容量使用情况
SELECT
    store_id,
    capacity,
    available,
    available / capacity as available_ratio,
    region_size,
    leader_size
FROM information_schema.TIDB_STORE_STATUS;
```

## 6. 高可用和容灾

### 6.1 故障检测和恢复

#### 6.1.1 故障检测机制

**检测策略**：
- **心跳检测**：定期发送心跳包
- **超时机制**：超时未响应视为故障
- **多重确认**：多个节点确认故障状态

**故障类型**：
- **节点故障**：单个 TiKV 或 TiDB 节点故障
- **网络分区**：节点间网络通信中断
- **存储故障**：磁盘损坏或数据丢失

```sql
-- 查看故障检测状态
SELECT
    store_id,
    state,
    last_heartbeat,
    uptime
FROM information_schema.TIDB_STORE_STATUS
WHERE state != 'Up';

-- 查看故障恢复进度
SELECT * FROM information_schema.TIDB_RECOVERY_STATUS;
```

#### 6.1.2 自动故障恢复

**恢复流程**：
1. 检测到节点故障
2. PD 标记故障节点
3. 重新选举新的 Region Leader
4. 数据自动同步到健康节点

**恢复时间**：
- Leader 选举：通常 1-3 秒
- 数据同步：取决于数据量
- 完全恢复：几分钟到几小时

```sql
-- 监控恢复状态
SELECT
    region_id,
    peer_id,
    is_learner,
    is_leader,
    state
FROM information_schema.TIDB_REGION_PEERS
WHERE state != 'Normal';
```

### 6.2 数据一致性保证

#### 6.2.1 强一致性机制

**Raft 协议保证**：
- 写操作需要多数节点确认
- Leader 切换保证数据不丢失
- 线性一致性读写

**一致性验证**：
- 定期数据校验
- 自动修复不一致数据
- 防止脑裂情况

```sql
-- 验证数据一致性
CHECK TABLE your_table;

-- 查看数据校验状态
SELECT * FROM information_schema.TIDB_CONSISTENCY_CHECK;
```

#### 6.2.2 数据备份和恢复

**备份策略**：
- **全量备份**：定期完整数据备份
- **增量备份**：备份变更的数据
- **实时备份**：基于变更数据捕获

**恢复策略**：
- **时间点恢复**：恢复到特定时间点
- **表级别恢复**：恢复单个表
- **集群级恢复**：恢复整个集群

```bash
# 全量备份
tiup br backup full \
    --pd "127.0.0.1:2379" \
    --storage "s3://backup-data" \
    --ratelimit 128

# 增量备份
tiup br backup full \
    --pd "127.0.0.1:2379" \
    --storage "s3://backup-data" \
    --lastbackupts "2024-01-01 00:00:00"

# 恢复数据
tiup br restore full \
    --pd "127.0.0.1:2379" \
    --storage "s3://backup-data" \
    --ratelimit 128
```

## 7. 性能优化

### 7.1 读写性能优化

#### 7.1.1 写入优化

**批量写入**：
```sql
-- 批量插入代替单条插入
INSERT INTO orders (user_id, order_no, total_amount) VALUES
(1, 'ORDER001', 100.00),
(2, 'ORDER002', 200.00),
(3, 'ORDER003', 300.00);

-- 使用 LOAD DATA 导入大量数据
LOAD DATA LOCAL INFILE '/path/to/data.csv'
INTO TABLE orders
FIELDS TERMINATED BY ','
LINES TERMINATED BY '\n'
(user_id, order_no, total_amount);
```

**连接池优化**：
```java
// Java 连接池配置示例
HikariConfig config = new HikariConfig();
config.setJdbcUrl("jdbc:mysql://127.0.0.1:4000/test");
config.setUsername("root");
config.setMaximumPoolSize(20);
config.setMinimumIdle(5);
config.setConnectionTimeout(30000);
config.setIdleTimeout(600000);
config.setMaxLifetime(1800000);
```

#### 7.1.2 读取优化

**索引优化**：
```sql
-- 创建合适的索引
CREATE INDEX idx_user_created ON orders(user_id, created_at);

-- 使用覆盖索引
CREATE INDEX idx_order_summary ON orders(user_id, total_amount, created_at);

-- 避免全表扫描
SELECT * FROM orders WHERE user_id = 1 AND created_at >= '2024-01-01';
```

**查询优化**：
```sql
-- 使用分区表减少扫描范围
CREATE TABLE orders_2024 PARTITION BY RANGE (YEAR(created_at)) (
    PARTITION p2024 VALUES LESS THAN (2025)
);

-- 使用 TiFlash 加速分析查询
ALTER TABLE orders SET TIFLASH REPLICA 1;
SELECT /*+ TIFLASH() */ user_id, SUM(total_amount) FROM orders GROUP BY user_id;
```

### 7.2 监控和调优

#### 7.2.1 性能监控

**关键指标监控**：
```sql
-- 慢查询监控
SELECT
    query_time,
    digest_text,
    count(*) as execution_count
FROM information_schema.CLUSTER_SLOW_QUERY
WHERE query_time > 1000000
GROUP BY digest_text
ORDER BY avg_time DESC
LIMIT 10;

-- QPS 监控
SELECT
    SUM(read_query_count) as read_qps,
    SUM(write_query_count) as write_qps,
    SUM(read_query_count + write_query_count) as total_qps
FROM information_schema.TIDB_METRICS_TABLE;
```

**Grafana 监控面板**：
- **Overview**：集群概览
- **TiDB**：SQL 层性能
- **TiKV**：存储层性能
- **PD**：调度器状态

#### 7.2.2 配置优化

**TiDB 配置优化**：
```ini
[tidb]
# 内存配置
performance.memory-usage-control = true
performance.max-memory = "32GB"

# 连接配置
max-connections = 1000
connection-timeout = "30s"

# 查询配置
max-execution-time = 3600000
```

**TiKV 配置优化**：
```ini
[tikv]
# 存储配置
storage.block-cache.capacity = "16GB"
storage.write-buffer-size = "128MB"

# Raft 配置
raft.heartbeat-interval = "20ms"
raft.election-timeout = "1000ms"
```

## 8. 总结

TiDB 的分布式特性提供了：

- **自动分片**：数据自动分布和扩展
- **强一致性**：基于 Raft 协议的数据一致性
- **高可用性**：自动故障检测和恢复
- **弹性扩展**：在线扩缩容能力
- **负载均衡**：智能调度和负载分散
- **HTAP 能力**：同时支持事务和分析

理解这些分布式特性，有助于更好地设计应用架构和优化性能。在实际应用中，需要根据业务场景选择合适的配置和优化策略。

---

**下一步学习**：学习 TiDB 的 HTAP 混合事务分析处理功能。